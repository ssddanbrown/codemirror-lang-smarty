# Test Booleans

{true false}

==>

Template(CodeBlock(Boolean, Boolean))

# Test Numbers

{5 0.1 3 -5.31554}

==>

Template(CodeBlock(Number, Number, Number, Number))

# Test Identifiers

{$one $Two_Three}

==>

Template(CodeBlock(Identifier, Identifier))

# Test Chained Identifiers

{$one.two.Three}

==>

Template(CodeBlock(ChainedIdentifier))

# Test Strings

{"hello" "one\"two" 'whoah there!' 'nes\'sy'}

==>

Template(CodeBlock(String, String, String, String))

# Test Comparison

{"nor" or "Goodbye"}

==>

Template(CodeBlock(String, Comparison, String))

# Test Functions

{cat() hello(5, false)}

==>

Template(
  CodeBlock(
    Function(Identifier, FunctionParamList()),
    Function(Identifier, FunctionParamList(Number, Boolean))
  )
)

# Test Array

{["cat", 5, true]}

==>

Template(
  CodeBlock(
    Array(
      String, Number, Boolean
    )
  )
)

# Test Mix of types

{-5.4 hello(5, "dog") true donkey "false" "-7.4"}

==>

Template(
  CodeBlock(
    Number,
    Function(Identifier, FunctionParamList(Number, String)),
    Boolean, Identifier, String, String
  )
)